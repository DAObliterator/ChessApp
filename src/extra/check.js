/*

let blah = Pieces.find((obj) => obj.position === tile.id);

    let accesiblePositions2 = tilesAccesible(
      blah.position,
      blah.color,
      blah.name,
      OccupiedByBlacksArray,
      OccupiedByWhitesArray
    );

    for (const i of accesiblePositions2) {
      let obj = Pieces.find((obj) => obj.position === i);
      if (obj) {
        if (obj.name === "K") {
          console.log(`Check -😲- \n`);
        }
      }
    }
    
*/

/* before the changes are made , thats reflected in the board  */


